#cloud-config
package_upgrade: true
packages:
  - git
  - curl
  - ca-certificates

runcmd:
  # 1. Install official Docker Engine & Docker Compose Plugin
  - install -m 0755 -d /etc/apt/keyrings
  - curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
  - chmod a+r /etc/apt/keyrings/docker.asc
  - echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo "$VERSION_CODENAME") stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null
  - apt-get update
  - apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
  - systemctl enable docker
  - systemctl start docker

  # 2. Clone repository & bring up HMS stack
  - mkdir -p /opt/hms
  - git clone -b qasimkhanfaridi-turbo-funicular https://github.com/qasimkhanfaridi/AAQSOLS-HeartClinic-HMS.git /opt/hms
  - cd /opt/hms
  - docker compose up -d --build
