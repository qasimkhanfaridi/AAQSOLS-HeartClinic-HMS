services:
  - type: web
    name: heartclinic-hms-api
    runtime: docker
    plan: free
    region: oregon
    dockerfilePath: ./Dockerfile
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Demo
      - key: UseSqlite
        value: "true"
      - key: PORT
        value: 8080
